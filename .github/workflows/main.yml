name: Main
on:
  push:
    branches:
      - master
  pull_request:
    types:
      - opened
      - ready_for_review
jobs:
  random-assignee:
    runs-on: ubuntu-latest
    steps:
      - uses: D1ceWard/random-assignee-action
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
