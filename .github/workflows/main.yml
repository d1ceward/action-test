name: Main
on:
  push:
    tags:
      - 'v*'
    branches:
      - release-test
jobs:
  release-test:
    name: Release-test
    runs-on: ubuntu-latest
    steps:
      - name: Test upload release
        uses: d1ceward/release-upload-action@master
